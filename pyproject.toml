[project]
name = "cute-dsl-cookbook"
version = "0.1.0"
description = "Python environment for CuTe DSL experiments"
requires-python = ">=3.12,<3.13"
dependencies = [
    "jupyter",
    "mypy==1.19.1",
    "nvidia-cutlass-dsl[cu13]==4.6.1",
    "torch",
]

[tool.uv]
package = false
